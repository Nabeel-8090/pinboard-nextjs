version: '3.8'

services:
  app:
    build: .
    container_name: pinboard-app
    restart: always
    ports:
      - "80:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
